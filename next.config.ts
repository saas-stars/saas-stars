import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Static export for Netlify/Vercel
  // Remove this if you want server-side rendering on Vercel
  // output: "export",
};

export default nextConfig;
